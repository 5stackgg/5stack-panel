# See overlays/http/ingress-patch.yaml: the Certificate is declared explicitly
# in certificate.yaml rather than left to cert-manager's ingress-shim.
- op: add
  path: /spec/tls
  value:
    - hosts:
        - HOST
      secretName: 5stack-mediamtx-ssl
