apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  namespace: 5stack
  name: 5stack-mediamtx-ssl
spec:
  secretName: 5stack-mediamtx-ssl
  renewBefore: 240h
  duration: 2160h
  dnsNames:
  - ${GAME_STREAM_DOMAIN}
  issuerRef:
    name: 5stack-issuer
    kind: Issuer
