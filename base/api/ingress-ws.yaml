apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: ws
  namespace: 5stack
spec:
  entryPoints:
    - web
    - websecure
  routes:
    - match: Host(`$(WS_DOMAIN)`)
      kind: Rule
      services:
        - name: api
          port: 5585
      middlewares:
        - name: rewrite-ws
  tls:
    secretName: 5stack-ssl