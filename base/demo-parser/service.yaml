apiVersion: v1
kind: Service
metadata:
  name: demo-parser
  namespace: 5stack
  labels:
    app: demo-parser
spec:
  type: ClusterIP
  selector:
    app: demo-parser
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
